{
  "name": "mono-template",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "watch:sass": "sass sass/main.scss style.css -w",
    "start": "npm-run-all --parallel dev watch:sass"
  },
  "devDependencies": {
    "sass": "^1.35.1",
    "vite": "^2.3.8"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
